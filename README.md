# arduino-pot-ldr-led-selector
A potentiometer selects which of 4 LEDs is active by dividing its range into 4 zones. The active LED's brightness is controlled by an LDR (light sensor). A button toggles a special mode where all 4 LEDs are controlled by the potentiometer simultaneously, with a buzzer notifying you when special mode is on.
